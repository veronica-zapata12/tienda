server:
  port: 8095
spring:
  profiles:
    active: master
  cloud:
    config:
      server:
        git:
          uri: https://github.com/veronica-zapata12/tienda.git
          searchPaths: config-data
          skipSslValidation: true

  security:
    user:
      name: root
      password: s3cr3t
