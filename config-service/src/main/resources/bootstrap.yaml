server:
  port: 8083

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/veronica-zapata12/tienda.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      user: root
      password: s3cr3t
