server:
  port: 8095
sipring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/veronica-zapata12/tienda.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user: root
    passord: s3cr3t